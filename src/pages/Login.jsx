import React from 'react';
import '../css/pages.css';

const Login = () => (
  <div className="page">
    <h2>Login</h2>
    <p>Authentication UI here (Firebase Auth recommended).</p>
  </div>
);

export default Login;
